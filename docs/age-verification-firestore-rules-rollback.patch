diff --git b/firestore.rules a/firestore.rules
index 92422d7..d2629af 100644
--- b/firestore.rules
+++ a/firestore.rules
@@ -2,17 +2,8 @@ rules_version = '2';
 service cloud.firestore {
   match /databases/{database}/documents {
 
-    function authenticated() {
-      return request.auth != null;
-    }
-
-    // Áreas sociais exigem confirmação 18+ emitida exclusivamente por Function.
-    // O próprio users/{uid} continua legível para a tela de verificação.
     function signedIn() {
-      return authenticated()
-        && exists(/databases/$(database)/documents/users/$(request.auth.uid))
-        && get(/databases/$(database)/documents/users/$(request.auth.uid))
-          .data.get('ageVerificationStatus', '') == 'verified';
+      return request.auth != null;
     }
 
     function uid() {
@@ -47,7 +38,7 @@ service cloud.firestore {
     }
 
     function isPortalAdmin() {
-      return authenticated()
+      return signedIn()
         && exists(/databases/$(database)/documents/admins/$(request.auth.uid));
     }
 
@@ -137,13 +128,7 @@ service cloud.firestore {
         'invitedByCode',
         'role',
         'roles',
-        'permissions',
-        'dateOfBirth',
-        'ageVerificationStatus',
-        'agePolicyVersion',
-        'ageTermsAcceptedAt',
-        'ageVerifiedAt',
-        'ageVerificationCheckedAt'
+        'permissions'
       ];
     }
 
@@ -167,15 +152,7 @@ service cloud.firestore {
 
     function isUserCreatePremiumSafe() {
       let d = request.resource.data;
-      return (!d.keys().hasAny([
-          'dateOfBirth',
-          'ageVerificationStatus',
-          'agePolicyVersion',
-          'ageTermsAcceptedAt',
-          'ageVerifiedAt',
-          'ageVerificationCheckedAt'
-        ]))
-        && (!('premiumUntil' in d))
+      return (!('premiumUntil' in d))
         && (!('premiumType' in d) || d.premiumType == null || d.premiumType == '')
         && (!('premiumSource' in d) || d.premiumSource == null || d.premiumSource == '')
         && (!('premiumExpiresAt' in d))
@@ -256,10 +233,10 @@ service cloud.firestore {
     // USERS
     // =========================
     match /users/{userId} {
-      allow read: if (authenticated() && uid() == userId) || signedIn();
+      allow read: if signedIn();
 
       // Create: defaults ok; não permitir auto-concessão Premium/convite.
-      allow create: if authenticated()
+      allow create: if signedIn()
         && uid() == userId
         && isUserCreatePremiumSafe();
 
@@ -311,12 +288,9 @@ service cloud.firestore {
       allow read: if signedIn();
       // Cliente pode criar/atualizar perfil público, mas NÃO pode forçar
       // isOnline:false no agregado (offline do agregado = Cloud Function / TTL).
-      allow create: if signedIn()
-        && uid() == userId
-        && !request.resource.data.keys().hasAny(['dateOfBirth', 'age']);
+      allow create: if signedIn() && uid() == userId;
       allow update: if signedIn()
         && uid() == userId
-        && !request.resource.data.keys().hasAny(['dateOfBirth', 'age'])
         && (
           !('isOnline' in request.resource.data.diff(resource.data).affectedKeys())
           || request.resource.data.isOnline == true

