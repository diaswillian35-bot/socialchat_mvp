import 'package:flutter/material.dart';

class AdminOfflinePage extends StatelessWidget {
  const AdminOfflinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Admin Offline')),
      body: const Center(
        child: Text('AdminOfflinePage (placeholder)'),
      ),
    );
  }
}